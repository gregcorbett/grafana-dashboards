apiVersion: 1

datasources:
  - name: Elasticsearch
    type: elasticsearch
    url: http://${hostname}:9200
    database: "[accounting-records-*-]YYYY.MM.DD"
    jsonData:
        esVersion: 60
        interval: Daily
        timeField: "@timestamp"
#        tlsAuthWithCACert: true
    secureJsonData:
        tlsCACert: |
          # Yes, you actually have to copy the contents of 
          # /etc/grid-security/certificates/UKeScienceRoot-2007.pem
          # into this yaml file...
          # Omitted for now
